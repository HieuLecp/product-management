const socket= io();

// let socket;

// window.addEventListener("load", () => {
//     if (socket) {
//         socket.disconnect();
//     }
//     socket = io(); // Tạo kết nối WebSocket mới
// });

// var socket = io({ reconnection: false }); 
