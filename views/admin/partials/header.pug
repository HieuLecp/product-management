header.header 
    .container-fluid
        .row.align-items-center 
            .col-3 
                .inner-logo
                    a(href="/admin/dashboard") ADMIN
            
            .col-9 
                div(class="text-right d-flex justify-content-end align-items-center gap-3")
                    a(
                        href=`${prefixAdmin}/my-account`
                        class="btn btn-primary btn-sm mr-2 butt-info "
                    ) #{user.fullName}
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn btn-danger btn-sm butt-logout d-flex justify-content-end"
                    ) Đăng xuất
                