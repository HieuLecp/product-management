mixin formChangeMulti(path)
    if(role.permissions.includes("products_edit"))
        form(
            action=path
            method="POST"
            form-change-multi
        )
            .d-flex.align-items-start
                .form-group
                    select(name="type" class="form-control")
                        option(disabled selected) -- Chọn hành động --
                        option(value="active") Hoạt động
                        option(value="inactive") Dừng hoạt động
                        option(value="delete-all") Xoá tất cả
                        option(value="change-position") Sắp xếp vị trí
                .form-group 
                    input(
                        type="text"
                        name="ids" 
                        value=""
                        class="form-control d-none"
                    )
                button(type="submit" class="btn btn-primary") Áp dụng


mixin formChangeMultiBin(path)
    form(
        action=path
        method="POST"
        form-change-multi-bin
    )
        .d-flex.align-items-start
            .form-group
                select(name="type" class="form-control")
                    option(disabled selected) -- Chọn hành động --
                    option(value="delete-all-bin") Xoá tất cả
                    option(value="restore-all") Khôi phục tất cả
            .form-group 
                input(
                    type="text"
                    name="ids" 
                    value=""
                    class="form-control d-none"
                )
            button(type="submit" class="btn btn-primary") Áp dụng


mixin formChangeMultiOrder(path)
    form(
        action=path
        method="POST"
        form-change-multi-order
    )
        .d-flex.align-items-start
            .form-group
                select(name="type" class="form-control")
                    option(disabled selected) -- Chọn hành động --
                    option(value="accept-all") Duyệt đơn
                    option(value="prepare-all") Chuẩn bị hàng
                    option(value="shipp-all") Giao hàng
            .form-group 
                input(
                    type="text"
                    name="ids" 
                    value=""
                    class="form-control d-none"
                )
            button(type="submit" class="btn btn-primary") Áp dụng